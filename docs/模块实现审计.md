# 模块实现审计

审计日期：2026-08-05。范围为 `MxtDatapackRegistries` 当前注册的 29 个原版数据包注册表。

判定标准：

- **完成**：框架可解码、引用并查询定义，且测试模组提供可加载的示例。是否注册具体物品、方块、命令、菜单或世界生成，属于内容模组与脚本的职责，不作为框架缺口。
- **部分完成**：定义宣称提供的框架能力本身尚无消费者。

## 修炼与角色

### 资源系统

| 注册表 | 数据驱动内容 | 状态 | 依据与缺口 |
| --- | --- | --- | --- |
| `resource` | 初始值、上下限、再生公式、吸收灵气时同步恢复开关、默认资源条、首个境界 | 完成 | 实体附件、再生、HUD 初始化与境界缓存均会读取；`restore_on_absorb` 可单独控制该资源是否在修炼吸收灵气时恢复。 |
| `resource_bar` | 资源、锚点、排序、可见性、渲染器、上下文 | 完成 | 自我、准星目标与 Boss 位置均会渲染；同资源同上下文的 `replace_default` 会覆盖普通声明。 |

### 境界系统

| 注册表 | 数据驱动内容 | 状态 | 依据与缺口 |
| --- | --- | --- | --- |
| `realm_stage` | 强绑定灵气、线性下一境界、阈值、条件、消耗、能力需求、天劫、成败动作 | 完成 | `CultivationService` 和服务器境界缓存会验证并推进线性链。 |

### 元素系统

| 注册表 | 数据驱动内容 | 状态 | 依据与缺口 |
| --- | --- | --- | --- |
| `element` | 克制、适应、灵气标签 | 完成 | 克制已提供通用条件；适应与标签作为可供内容方读取的元素元数据保留。 |

### 灵根系统

| 注册表 | 数据驱动内容 | 状态 | 依据与缺口 |
| --- | --- | --- | --- |
| `spirit_root` | 单元素绑定、修炼/技能公式、能力引用、稀有度、兼容标签 | 完成 | 元素、修炼倍率、技能倍率与授予能力已接入；兼容标签供内容规则筛选。 |

### 体质系统

| 注册表 | 数据驱动内容 | 状态 | 依据与缺口 |
| --- | --- | --- | --- |
| `physique` | 属性、能力、持有条件、互斥标签、稀有度、叠加规则 | 完成 | 授予、条件与被动属性刷新均已接入。 |

### 技能系统

| 注册表 | 数据驱动内容 | 状态 | 依据与缺口 |
| --- | --- | --- | --- |
| `ability` | 类型分派、触发器、条件、资源消耗、公式值、动作、冷却与持续状态 | 完成 | 事件桥接、附件、网络调用和被动属性均已接入。 |

### 诅咒系统

| 注册表 | 数据驱动内容 | 状态 | 依据与缺口 |
| --- | --- | --- | --- |
| `curse` | 类型、时长、堆叠、应用条件、应用/周期/移除动作、净化标签 | 完成 | 诅咒附件、调度器、动作和战利品函数均已接入。 |

### 功法系统

| 注册表 | 数据驱动内容 | 状态 | 依据与缺口 |
| --- | --- | --- | --- |
| `cultivation_technique` | 学习条件、能力、属性、资源与境界要求 | 完成 | 授予服务、修炼行为和被动属性均会读取。 |

### 修炼行为系统

| 注册表 | 数据驱动内容 | 状态 | 依据与缺口 |
| --- | --- | --- | --- |
| `cultivate_action` | 开始/停止条件、周期、环境、资源消耗与获得、灵气消耗、周期动作 | 完成 | 修炼事件桥接会执行条件、资源与动作。 |

### 称号系统

| 注册表 | 数据驱动内容 | 状态 | 依据与缺口 |
| --- | --- | --- | --- |
| `title` | 解锁条件、优先级、属性、互斥标签、等级上限 | 完成 | 被动属性和通用授予服务已提供；如何触发解锁由内容方决定。 |

## 锻造、炼丹与数据物品

### 锻造系统

| 注册表 | 数据驱动内容 | 状态 | 依据与缺口 |
| --- | --- | --- | --- |
| `forging_method` | 锻打数值变化、条件 | 完成 | 锻造工作台服务与网络负载会校验并应用。 |
| `forging_blueprint` | 目标物品、合格区间、末步匹配、最优步数、成败动作 | 完成 | 锻造会话与工作台服务会计算结果并执行动作。 |

### 法器系统

| 注册表 | 数据驱动内容 | 状态 | 依据与缺口 |
| --- | --- | --- | --- |
| `item_archetype` | 法器类型、灵力容量、储物、飞行、成本、能力、精炼行为 | 完成 | 法器存储、飞行、能力与精炼行为桥接均已提供；精炼保留领域行为接口，因为它缺少统一动作目标。 |

### 灵植与材料品级系统

| 注册表 | 数据驱动内容 | 状态 | 依据与缺口 |
| --- | --- | --- | --- |
| `spirit_herb` | 现有物品/标签绑定、品级、年龄、元素/材料标签、生长率、掉落率 | 完成 | 通过 `items` 绑定现有物品，`SpiritHerbService` 提供服务端与客户端查询；种植、采集、掉落和生成由内容方实现。 |
| `material_grade` | 层级、价值/锻造/炼丹公式、适用标签 | 完成 | 作为灵植和内容方物品的通用质量元数据，由 Holder 引用并可按需读取修正公式。 |

### 炼丹系统

| 注册表 | 数据驱动内容 | 状态 | 依据与缺口 |
| --- | --- | --- | --- |
| `alchemy_recipe` | 输入、温度、炉阶、时长、环境、灵气、产物、成败行为 | 完成 | 会话和工作台服务可校验环境并产出；成败行为使用适合无实体上下文的领域行为接口。 |

### 物品绑定系统

| 注册表 | 数据驱动内容 | 状态 | 依据与缺口 |
| --- | --- | --- | --- |
| `item_binding` | 已注册物品到食用完成后动作数组的绑定 | 完成 | 不创建或保存逻辑物品定义；KubeJS 和其他模组物品可直接引用，内置 `grant_spirit_root` 动作用于授予灵根。 |
| `weapon_binding` | 已注册物品的攻击属性、右键、攻击、持有 tick 动作 | 完成 | 武器字段直接写在专用绑定中，避免与通用效果混用。 |
| `pill_binding` | 已注册物品的服用、丹毒与过量规则 | 完成 | 丹药字段直接写在专用绑定中。 |

## 世界、阵法与生物

### 灵气环境系统

| 注册表 | 数据驱动内容 | 状态 | 依据与缺口 |
| --- | --- | --- | --- |
| `aura_zone` | 维度/群系匹配、元素灵气、波动、规则、噪声、渲染配置 | 完成 | 服务端查询、区块缓存、阵法覆写和事件已接入；客户端会按数据包渲染粒子、雾色与雾距。 |
| `block_aura` | 方块/方块标签匹配、每方块灵气与元素贡献、环境标签 | 完成 | 区块加载与变更后重建缓存，支持混合方块和标签引用。 |

### 阵法系统

| 注册表 | 数据驱动内容 | 状态 | 依据与缺口 |
| --- | --- | --- | --- |
| `formation` | 结构、半径、维护资源、激活/tick/失效方块动作、范围实体动作、灵气覆写 | 完成 | 生命周期和范围 `entity_tick_action` 已运行；测试模组在启动时构造原版一格模板并验证数据包阵法匹配。 |

### 天劫系统

| 注册表 | 数据驱动内容 | 状态 | 依据与缺口 |
| --- | --- | --- | --- |
| `tribulation` | 触发条件、阶段时长、阶段动作、难度、成败动作 | 完成 | 突破服务会启动天劫并由事件桥接执行阶段。 |

### 生物档案系统

| 注册表 | 数据驱动内容 | 状态 | 依据与缺口 |
| --- | --- | --- | --- |
| `creature_profile` | 生物类型/标签、灵气元素、最低灵气、智力、内丹、掉落表、境界、契约标签 | 完成 | 类型与灵气筛选、智力、内丹、掉落表会运行；其余字段作为内容方扩展的档案元数据保留。 |

### 契约系统

| 注册表 | 数据驱动内容 | 状态 | 依据与缺口 |
| --- | --- | --- | --- |
| `contract_type` | 主人/灵宠条件、跟随、战斗、解除、惩罚动作 | 完成 | 已提供绑定、解除、跟随、战斗与死亡动作服务；契约道具和界面由内容方决定。 |

### 秘境实例系统

| 注册表 | 数据驱动内容 | 状态 | 依据与缺口 |
| --- | --- | --- | --- |
| `realm_instance` | 目标维度、时长、人数、进入/离开动作 | 完成 | 跨维度进入、返回、过期清理与动作服务已实现；进入入口由内容方提供。 |

## 宗门与经济

### 宗门系统

| 注册表 | 数据驱动内容 | 状态 | 依据与缺口 |
| --- | --- | --- | --- |
| `sect` | 职级、贡献、任务、兑换、权限、领地权限 | 完成 | 宗门服务支持成员、贡献、晋升、任务、兑换与领地事务；界面和命令由内容方提供。 |

### 经济系统

| 注册表 | 数据驱动内容 | 状态 | 依据与缺口 |
| --- | --- | --- | --- |
| `currency` | 物品/标签/数据物品货币匹配、面值、兑换选项 | 完成 | 价值、找零、交易站输入和提示均已接入；实际兑换物由内容数据包定义。 |

## 测试数据覆盖

测试模组 `src/test-mod` 为上述 29 个注册表各提供至少一个 JSON。此次补充：

- `ability/firebolt.json`：以直接内联字段覆盖求和、均匀随机、二项分布和加权选择的嵌套解析。
- `creature_profile/spirit_wolf.json`：补充狼实体、实体标签、元素引用/标签、最低灵气和内丹，以覆盖实际选择与应用路径。
- `currency/iron_nugget.json`：覆盖物品与物品标签混合匹配。
- `spirit_herb/fire_ginseng.json`：覆盖现有物品与物品标签混合绑定，不创建专属灵植物品。
- `resource_bar/qi_target_*.json` 与 `spirit_power_boss.json`：覆盖目标条、Boss 位置条和同资源替换规则。
- `MxtTestMod`：服务端启动时验证灵气视觉配置、灵植物品匹配和原版结构模板阵法校验。
- `tags/entity_type/spirit_wolves.json`、`tags/item/currency_nuggets.json`、`tags/mxt/element/warm_elements.json`：为上述标签引用提供可解析成员。

JSON 已通过 PowerShell `ConvertFrom-Json` 校验；完整项目构建结果以本次修改后的 IDEA rebuild 为准。
