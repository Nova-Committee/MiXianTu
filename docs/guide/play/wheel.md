---
title: 轮盘、资源条与灵气 HUD
---

按住「轮盘选择」（默认 `R`），屏幕中央出现一个 **12 扇**的轮盘：**指针朝哪个方向就选中哪一扇**，那一扇变金色并向外扩一点，**它的名字写在轮盘正中间**、下面一行写着「按 `V` 使用」（键名跟着你的改键走）。每个扇区里画条目的图标；**条目没有图标就画名字的开头几个字**（宽度按那一扇的弧长算，不会压到隔壁扇区；完整名字始终在正中间）。环的上方还有一行写着**现在是第几页**：「轮盘 2/3：主手物品（两把切换键的实际绑定）」。

**轮盘是"主盘 + 从盘"，格子用一套连续编号串起来**：

- **主盘**就是你自己摆的那 12 格，编号 `0..11`；**每次按 `R` 打开都回到它**。
- **从盘**不能自己摆、也不需要摆：**主手物品**、**副手物品**、**法器**、**契约灵兽**，前三处的内容是装备**此刻**给你的主动技能、**加上它们给的、需要按键的技能**（法器给的储物、主动技这类；**飞行不在这里**——它是功法授予的御器之术），格子从 `12` 起接着排下去。换掉手里的剑、把法器摘下来，那几页当场就没了；装上去，技能与法器技能就都回来。**契约灵兽**那一页（2026-09-25 新增）读的是手里的**御兽铃**：铃右键一只自己的已契约灵宠就"对准"了它，再右键空处会直接**打开轮盘并停在这一页**，页上是这只灵兽认的命令（跟随 / 游荡 / 驻守 / 召回，内容方可以再加），点哪一格就下哪条命令；铃没对准任何灵宠时那一页是空的、右键空处会提示先用铃点一只。
- **一页 12 格**。某一处装备给的条目超过 12 个时**会自动再开一页**（15 个就是两页：第 2 页 3 格 + 9 个空格），没有条目会被丢掉。
- 用键盘的**左 / 右方向键**（`key.mxt.wheel_previous` / `key.mxt.wheel_next`，可以在按键设置里改绑）翻页，每切一次动作栏报一句「轮盘 2/3：主手物品」；**翻到头之后是绕回第一页还是停在两端，由客户端配置「轮盘选择 → 循环翻页」决定（默认绕回）**。
- **轮盘开着时，鼠标滚轮也能翻页**（向上＝上一页，向下＝下一页），和两把翻页键完全等价、同样报那一句。不用它就关掉客户端配置「轮盘选择 → 滚轮翻页」（**默认开**）；关着轮盘时滚轮不参与。
- **翻页轮盘开着关着都行**：关着翻是为了让 12 把槽位键换到那一页上；开着翻是为了翻页看。有别的界面开着（聊天栏、物品栏……）时不生效，和别的轮盘键一样。
- 因为页会跟着装备来去，**后面的编号会前后移动**——同一个编号在装备换掉之后可能指向另一个技能；这是有意的：**编号只是一个"第几格"的位置**，它自己不会因为你换了装备而改写。

**选择与使用是两个键**：

- `R`（`key.mxt.wheel`）**只负责选**：按住打开、指针指哪一扇就选哪一扇，**松开就关掉轮盘、不会触发**（同一页的「打开方式」可以让它变成按一下开、再按一下关）。
- `V`（`key.mxt.wheel_use`，新增，默认 `V`）**负责用**：轮盘开着时用掉指针当前指的那一格，**轮盘不关**，可以换一格接着按；轮盘关着时用掉**你上次选的那个编号此刻代表的那一格**。鼠标左键等同于 `V`。
- **永远有一个选中的格子**：一次都没选过时，选中落在**第一个有内容的格子**上（左侧「轮盘格」的金框一开始就画在那里），所以一按 `V` 就有目标；**指针停在空格子上不会把选择清掉**，它还留在你上一次真正选中的那一格。只有**整张轮盘一格内容都没有**时才没有目标。
- **物品拿走了也不会丢选择**：如果那个编号指向的格子已经不存在了（那一页没了，或者那一页的技能变少了），按 `V` 会自动落到**最后一个有东西的格子**上；**编号本身不改**，所以那把剑拿回来时，同一个编号又指向同一个技能。**这条只管"不存在"**：指针指着主盘上的空格子按 `V` 仍然什么都不做。
- 屏幕左侧（竖直居中）有一块「轮盘格」：**永远是 4 列，行数随内容的格子数往下长**，所以它是**整张轮盘的一览**，把主盘和从盘的格子都画出来。**只有主盘画空格子**——那 12 个空框是你自己摆的布局，空着就要看得见；从盘只画它真正有的技能（3 个技能就是 3 格，不会补一串空框）。格子里画图标或名字开头、底边一条色条（技能金 / 灵气蓝 / 法器技能：开关**开=绿、关=灰**，一次性的是**紫**）；**冷却中的格子按原版物品那样压一层白幕**（盖住图标的剩余比例、随时间从上往下退，与物品栏里那件东西同一套观感），其它原因不可用时压一层暗色。**编号此刻代表的那一格是金色边框**（那一格是空的就不点亮，等于没有目标），其余是灰框，空格子只有框。它显示的就是"现在按 `V` 会放什么"，也能一眼看到所有格里还放了什么。**块的上方不写任何字**：这是拿来看的一块，翻到哪一页由轮盘自己说。它和别的 HUD 元素一样能用 HUD 布局界面（默认右 `Shift`）拖动、用 `/hud wheel.selection reset` 或编辑器里的复位回默认（**编辑器里能拖动、拖到窗口边上的锚点方框上换锚点、也能用元素右上角的勾选框一键隐藏**），尺寸会自己跟着内容长——摆过旧版那一格的人在 `mxt-hud.json` 里还留着位置，用 `/hud wheel.selection reset` 或编辑器里的复位就能回到新的默认（左边中点）。
- **选中的编号会存下来**：下次登录时它原样回来，金框画在那个编号此刻代表的那一格上；那个位置上什么都没有也不会报错，也不会把编号清掉。
- **按键设置里多了一个「觅仙途：轮盘槽位」分类**：12 个槽位各一把键、**默认全部未绑定**（`槽位 1` 就是正上方那一格，顺时针数；列表按编号顺序排，不会出现 `1、10、11、12、2…`）。给某一格绑上键之后，**按下去就等于"把指针指到那一格再按 `V`"**——当场用掉一次，同时那一格成为当前选中（左侧轮盘格立刻变金框，编号也会跟着存档记下来），之后按 `V` 还能再放一次同一个。**它作用于当前那一页**（所以翻到主手物品那一页，槽位键放的就是那把剑的技能）。空格子按下什么都不发生、也不提示（和 `V` 的规矩一致）。在聊天栏里打字不会触发这些键。
- 在聊天栏里打 `v` 不会触发——只有没有任何界面打开（或轮盘正开着）时 `V` 才生效。
- **按了没生效会有一句动作栏提示**：**整张轮盘一格有内容的都没有**时按 `R` 或 `V` 都说「轮盘上还没有任何条目」（`R` 顺带告诉你先去 `/wheel`）。「从没选过格子」这一句已经不存在了——有内容时按 `V` 总会用掉那个兜底选中的格子。**指针指着的那一格是空的**时按 `V` 什么都不说——那一格在「轮盘格」上本来就画成空框，轮盘也不会关。有别的界面开着时（聊天栏、物品栏……）按键都不提示：那时你是在打字或翻背包，不是在用轮盘。
- **服务端拒绝时会说明原因**（2026-09-22 新增，2026-09-23 起说的是真原因）：按 `V`（或槽位键）之后如果服务端说"不行"，动作栏会报出来——「轮盘上的这一项已经失效了」（它已经不在那个来源上了，或者那个 id 现在解析不出技能）、「使用失败：<原因>」（需要按键的技能按不动时）与「施放失败：<原因>」（不是那种技能、走普通施放时），后两句接的是**同一份原因表**：灵根与元素不符 / 某门资源不足（会点名是哪一门）/ 还在冷却 / 条件不满足 / 次数用完 / 没有权限 / 数值配置有误 / 状态已经是这样 / 这件法器不认你 / 需要一件承载它的物品 / 现在骑不上去……；灵气发不出去时报「灵气没能发射出去」。日志里同时留一条 info，带原因名和缺的那门资源。之前这些失败是**完全静默**的，"按了没反应"就是全部症状；而 2026-09-23 之前按压那一侧只会报冷却 / 代价 / 未持有三种，别的原因一律显示「现在用不了」——现在这一句只剩真的说不清时才出现（例如拿不到服务端玩家）。

**需要按键的技能（2026-09-22 起；法器技能与技能已合并为同一种条目）。** 有一类技能**必须按键才发动**，它们和其它条目一样落在轮盘上——今天有三样，跟着承载它的东西走：

- **施放**：已经学会的主动技（`mxt:active`）按一下就是施放一次，和以前一样。
- **御器之术**（`mxt:flight_control`，2026-09-25 起由功法这类授予来源给，**不是法器自己声明的**）：学会之后轮盘上就多出一格「御器之术」，**按 `V` 从主手、其次副手取一件声明了载具（`mxt:mount`）的法器起剑，再按一次落剑**（法器原样回到背包），不需要另设快捷键。开关格子**底边绿色 = 开着、灰色 = 关着**，tooltip 写清「状态：已开启 / 已关闭」。飞行中：`跳跃`上升、**下降键（默认 `X`，可改绑）下沉**、`疾跑`×1.5，前后左右默认沿视线方向（服务端配置「飞行 → 朝视线方向飞行」），原版的**潜行＝下坐骑**照旧。
- **储物**：法器声明了储物格数时，那一页会多出一格「储物」，**按 `V` 打开这件法器自己的箱子**（窗口标题是技能名，标准箱子界面，一行九格、最多六行 = 54 格）；关掉窗口什么都不留下，所以那一格是**紫色**的、没有状态行。
- **契约命令**（2026-09-25 新增）：手里的御兽铃对准了一只灵宠时，它那一页会列出这只灵兽认的命令——**跟随 / 游荡 / 驻守**是常驻的（绿色，换一条就覆盖上一条），**召回**是一次性的（黄色，执行完当前命令不变）。命令不接受时动作栏照实报原因（"它不接受这道命令" / "它拒绝了这道命令"），日志同样留一条。

格子里写的是**技能名**（「御器之术」「储物」）而不是物品图标：同一件法器可能两样都有，画同一个图标就分不出谁是谁；**是哪件法器**在 tooltip 里写着。这些格子能不能**钉到主盘**看它在哪：**Curios 法器槽**里的法器，它的技能就在配置界面右边那一池里，钉上去之后只要那件法器还在双手或法器槽里就一直好用（放进背包里不算"在身上"）；**只拿在手里**的法器，它的技能只在"主手物品 / 副手物品"那一页上，不进池子——手里的东西说换就换。开关的开与关由**服务端**说了算：客户端只报告"你按了这一格"，服务端问一遍现在的状态再决定，所以画面上的状态和实际一定一致。储物窗口打开后，**承载它的那件物品一旦离开你身上（丢掉、被拿走）窗口就自己关掉**，免得往一个不在手里的东西上写东西。

**轮盘是技能（含法器技能）与灵气唯一的触发入口。** 用掉一个技能 = 施放一次；用掉一个灵气 = 发射一发灵力射线；用掉一个法器技能 = 那件法器做它该做的事（飞行开关翻到反面：开 = 起剑，关 = 落剑；储物则打开它自己的箱子）；用掉一个**契约命令** = 给手里御兽铃对准的那只灵宠下一条命令（除了这个入口，它还有一个管理员入口 `/contract behavior`）。技能能否施放（授予、消耗、冷却、施法时间）与灵气能否发射（元素、使用条件、冷却、余量）**全部由服务端判定**，客户端只报告"用掉了哪个来源的哪一项"（服务端还会核对"这一项确实来自那个来源"，契约命令也一样：铃里的命令表与灵宠自己的记录都在服务端再查一遍）；冷却中的格子会变暗、中间写「冷却中 4.3s」（剩余秒数，永远一位小数；这时按 `V` 服务端照旧会拒绝）。原来「按住 `LAlt` 显示技能栏 / 按住 `V` 发射灵力」的那两个按键、以及画面上那条快捷栏**都已取消**——`V` 现在被新的「轮盘使用」占用，含义是"用掉当前选中的那一格"，不再直接发灵力；数字键 1–9 也不再被本模组拦截。

轮盘里放什么由玩家自己定：聊天栏打 `/wheel`，或按「打开轮盘配置」（`key.mxt.wheel_configuration`，**默认未绑定**，可以自己去按键设置里设一个）打开**轮盘配置界面**——左边 6 列是能发射的灵气、右边 6 列是**学到的主动技能与 Curios 槽位上法器声明的技能**（主手 / 副手物品自己的技能不在这里，它们只在两张从盘页上；各自滚动、悬停看 tooltip，工具提示的最后一行写着它是**学习的技能**还是**X的技能**（X 是承载它的物品名，按品质上色、没有品质就金色）），下面一排 12 格就是**主盘**的 12 格（**1 在正上方，顺时针**，两池共用）。左键点一个条目选中它，再点下面某一格放进去；不选条目直接点格子则清空该格；`Esc` 保存并关闭。格子和扇区一样：有图标画图标、没有就画名字开头，**格子放不下的部分在悬停 tooltip 里**。**只有主盘在这里编辑**：从盘的内容由随身装备决定，站在界面里看的和在战斗里看的不是同一份，所以界面不预览它们（**界面上没有说明文字**：原先写"从盘自动生成、用左右方向键切换"的那一行与另外三句提示已于 2026-09-26 删除，12 格因此上移，格子下方一行写着每个扇区各自的槽位按键：形如 `[Z]`，没绑定就不显示）。**新存档的轮盘是空的**（不会替你预填任何东西）：放什么完全由你决定，得先在 `/wheel` 里把条目点进 12 格。**整张轮盘一格有内容的都没有**时按 `R` **不会打开**（没东西可选就没有开的必要），所以新档第一次用之前先去配置界面放几样；左侧那块「轮盘格」这时是 12 个空框（一页三行），正好告诉你"现在按 `V` 没有目标"。

资源条和灵气浓度条由服务端同步状态驱动，客户端只负责绘制。服务端按服务端配置里的「灵气 → 同步周期」（默认每 5 tick）同步当前位置灵气，雾效和粒子不会反向决定灵气数值。
