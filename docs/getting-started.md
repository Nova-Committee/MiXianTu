---
sidebar_position: 2
title: 基本信息与安装
---

## 基本信息

| 项目 | 当前值 |
| --- | --- |
| Minecraft | `26.1.2` |
| NeoForge | `26.1.2.92` |
| Java | `25` |
| Mod ID | `mxt` |
| 版本 | `1.0-alpha.1` |
| 许可证 | `All Rights Reserved` |

项目仍未发布，数据包 Codec、附件和网络协议暂不承诺旧版本兼容。

## 前置要求

- NeoForge `26.1.2.92`。
- Curios API `15.0.0+26.1.2`，必需前置。
- KubeJS `26.1.2-8.0.4`，仅在使用脚本扩展时需要。
- JEI、Jade 为可选兼容模组。

Curios API 会随开发构建通过 Jar-in-Jar 方式打包，但开发环境仍建议按 Gradle 配置准备对应版本。

## 开发运行

```bash
gradlew runClient
gradlew runTestClient
gradlew runTestServer
```

`runTestClient` 加载 `src/main` 和 `src/test-mod`，用于手动验证数据包和客户端显示；测试模组不会打入正式 Jar。

## 模块完成度

- **完成**：注册表、字段和运行时消费者形成闭环。
- **制作中**：大部分字段已接入，但缺少必要消费者或跨模块闭环。
- **预留**：仅有数据结构、Codec 或客户端预留，没有完整运行时逻辑。

| 模块 | 状态 | 说明 |
| --- | --- | --- |
| 动态注册表与 Codec | 完成 | 原版数据包注册表、Holder 引用、禁用标签和类型分派。 |
| 资源与境界 | 完成 | 资源条、境界链、修炼资源和服务端同步。 |
| 灵气环境 | 制作中 | 灵气查询、区块缓存、方块/物品灵气和 HUD 已接入；灵植、炼丹和自然生成规则仍缺少完整消费者。 |
| 行为、条件与 Cost | 完成 | Entity/Item/Block 行为与条件分派，技能消耗由 Cost 结算。 |
| 物品绑定与经济 | 完成 | 通用、武器、丹药、功法绑定，货币与交易。 |
| 阵法、锻造、炼丹 | 完成 | 数据包定义和服务端工作流已接入。 |
| 技能 `ability` | 制作中 | 技能类型、触发器、组件、Cost、冷却、行为和网络已接入；元素亲和等扩展尚未统一结算。 |
| 灵植、秘境、生物档案 | 制作中 | 秘境已闭环，灵植生命周期和生物档案部分联动仍在补齐。 |
| Badge 与部分高级 KubeJS API | 预留 | 类型可注册，部分展示和脚本 API 尚未完整。 |
