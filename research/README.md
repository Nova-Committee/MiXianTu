# research/ —— 设计稿存档

**这里是本仓库放设计稿的地方——只有大改（新模块、换形状的改版、跨模块重构）或用户明确要求先设计时才写稿。**

代码只回答"现在是什么样"，`docs/` 回答"怎么用"，而**为什么这么设计、当时否掉了哪些方案、边界在哪里**
只活在这个目录里。所以真要做这类改动时先在这里落一份档；日常小改、修 bug、改名、拆包、加字段、文档同步
**不写稿**——结论留在代码注释（硬约束）与 `docs/`、[`AGENTS.md`](../AGENTS.md)（约定）里即可。没有留档的大改过一个版本就查不回来了。

**先看下面的[目录](#目录)。** 它按主题列出每份稿的一句话主旨与当前状态：**"这篇还能不能信"
看状态列；完成度永远以代码为准（两份 README 的「模块完成情况」表是汇总），不以研究稿为准。**

## 目录

状态口径：

| 状态      | 意思                                |
|---------|-----------------------------------|
| 现行      | 结论仍然成立。动这块之前先读它                   |
| 已落地     | 设计已经实现。读它是为了知道"为什么这么设计"，实现细节以代码为准 |
| 部分有效    | 文内已有作废标注，或留了"不做 / 推迟"段；按文内的修订节读   |
| 分析 / 研究 | 外部工程阅读、可行性分析或原版源码研究；未必产生代码        |
| 素材      | 不是设计稿（清单、备忘、脚本、配图）                |

### A. 总览、范围与工程约定

| 稿                    | 一句话                                                                                                            | 状态                                                       |
|----------------------|----------------------------------------------------------------------------------------------------------------|----------------------------------------------------------|
| `00_策划整理与范围.md`      | 起点的策划整理：目标、本期范围、明确不做、优先级、最小闭环                                                                                  | 现行（范围声明）                                                 |
| `01_模块划分与依赖.md`      | 工程模块划分、各自职责、关键运行时数据与依赖规则                                                                                       | 现行（工程约定）                                                 |
| `02_动态注册表清单.md`      | 该有哪些动态注册表、哪些内容不该做成注册表、KubeJS 合并规则；文首是逐条变更记录                                                                    | 现行（改注册表时同步；`talisman` 那两条结论已过时，见 `audit/生产系统审计.md` §4.2） |
| `03_功能清单与运行流程.md`    | P0–P3 功能清单、关键事件与推荐开发顺序                                                                                         | 早期排期稿：多数已落地，宗门等条目已作废                                     |
| `04_数据包与KubeJS边界.md` | 代码 / 数据包 / 脚本三层职责、KubeJS API 形状与性能底线                                                                           | 现行（边界声明）                                                 |
| `34_功法载体物品组件设计.md`   | 功法手册的载体从"按物品 id 绑定"改成物品组件 `mxt:technique`：字段增删（`items` → `carrier_item`）、本体按功法自动生成玉简、picker 与创造模式的落点、按堆解析与缓存口径 | 现行（2026-09-22 定案）                                        |
| `08_注册表协作规范.md`      | 领域 behavior 注册表已移除：可配置回调统一走 Action 系统（短篇）                                                                      | 现行                                                       |
| `11_注册表结构与引用关系图.md`  | 注册表之间引用关系的图（短篇）                                                                                                | 现行（图解）                                                   |
| `备忘录.md`             | 未来迁移高版本的备忘（26.2 / 26.3 的原版注册表变化）                                                                               | 素材                                                       |

### B. 数据定义与系统内核

| 稿                   | 一句话                                                                                                                                                                                                                                                                                                                                               | 状态                                                   |
|---------------------|---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|------------------------------------------------------|
| `05_技能系统与固有注册表.md`  | 能力（技能）的统一模型、数值公式、固有类型注册表与生命周期                                                                                                                                                                                                                                                                                                                     | 已落地（架构结论仍是现在的形状）                                     |
| `06_锻造系统设计.md`      | 锻造数值条、末尾序列、最优步数、品质结算与数据包草案                                                                                                                                                                                                                                                                                                                        | 部分有效：数据包草案字段已全部失效（`audit/生产系统审计.md` §4.2），落地形状见 `16` |
| `07_灵根与体质拆分设计.md`   | 灵根与体质的定义边界、数据模型、附件与能力授予                                                                                                                                                                                                                                                                                                                           | 已落地                                                  |
| `09_资源条注册表设计.md`    | `resource` / `resource_bar` 的数据模型、渲染器与统一资源成本格式                                                                                                                                                                                                                                                                                                    | 已落地                                                  |
| `10_诅咒动态注册表设计.md`   | `curse` 字段、解毒改由解毒剂 + 标签管理、运行时顺序与热重载                                                                                                                                                                                                                                                                                                               | 已落地（"诅咒"语义已泛化为状态）                                    |
| `33_Trigger系统设计.md` | Dispatcher / Trigger / Subscription 分层、订阅不入存档、重建器约定                                                                                                                                                                                                                                                                                               | 已落地；文末是持久化审计                                         |
| `36_子境界设计.md`       | `realm_stage` 的 `minor_stages`：子境界名列表只喂信息面板显示与公式变量 `minor_stage`，按本境界 `breakthrough_exp` 均分；含"凡人读 `NaN`"与"超阈值停在最后一重"两个口径取舍                                                                                                                                                                                                                        | 已落地（2026-09-23；同日改成"组件数组或整数"，见文首修订与 `37`）            |
| `37_名字与描述自动填充设计.md` | 定义文本字段的自动填充：`ContextNameCodec` / `ContextNameListCodec`（原 `NameMapCodec`，2026-09-23 拆成两个）+ `RegistryOps` 携带当前条目 id（`RegistryLoadTask$PendingRegistration` 两处注入），`item_quality` 的 `display_name`→`name` 与新增 `description`、`Modifier.description` 改为可选、`minor_stages` 改成组件数组或整数；"两套名字键"已被 `38` 作废                                                   | 已落地（2026-09-23；§3、§6 由 `38` 修订）                      |
| `38_定义文本字段全量应用.md`  | 把这套字段应用到 18 张注册表，并把 id 派生键统一成生成键那一条四段式（`<类别>.mxt.<命名空间>.<路径>`，破坏性迁移）；`api/NamedDefinition` 让显示路径直接读字段；`RecordCodecBuilder.group` 的 16 组件上限用 `MiscCodecs.pair` 绕开                                                                                                                                                                                  | 已落地（2026-09-23）                                      |
| `39_全局消耗定义设计.md`    | **使用消耗**（技能 / 突破 / 修炼 / 阵法 / 锻造 / 法器维护 / 飞行 / 灵气合成，共 11 个字段）的三套形状收编成一个元素形状（`mxt:resource` / `mxt:aura` / `mxt:item` / `mxt:js` + 简写）：付款者是 `LivingEntity`（不是 `Player`）＋支付通道、`charge` 一个方法 + `CostTransaction` 的 `plan`/`commit`、整组原子（草稿优先、回退兜底）；文首 §0 标明**货币（`currency` 的 `cost`、`value_multiplier`）与本稿无关**，§13 是落地记录（含第二轮把两处灵气 map 并进 `Cost` 的结果） | 已落地（2026-09-23；只编译，探针未实跑）                            |
| `46_品质链条与升级设计.md` | 品质系统整合：链条要有载体（新注册表 `mxt:quality_chain`：`tiers` 顺序 + `default` + 每步 `upgrades{costs,condition}`）、升级服务复用 `CostTransaction` 原子付款、入口给命令与 KubeJS，并把 4 张绑定表的 `quality_group` 标签**合并成链引用**；附带收尾零调用的 `tooltip_order`/`ordered()` 与 `technique.grade` 的并入选项 | **已落地**（2026-09-23；§7 记了六项拍板结果与两处补充；`compileJava`/`compileTestModJava`/`processTestModResources` 通过；**探针未实跑、升级界面与跳档未做**） |
| `49_灵根多元素与子境界条件设计.md` | 三件事一件一形状：**灵根多元素**（`SpiritRoot.element` 删除 → `elements` 列表，每项裸 id 或带 `weight`；权重是**占比**、读取时归一化，只进亲和与对立惩罚——其余读法一次定死为并集 / 能力加成只算一次 / 集合互斥 / 关系倍率不看权重）、**子境界条件**（不新增类型，给 `mxt:realm` 加 `min_minor_stage`，读只增不减的层数记录）、**境界按子境界解锁 ability**（`minor_stage_abilities`，累计且永久保留，与功法的 `configuration` 同一口径） | **已落地**（2026-09-25；三处编译通过、lang 键未变、文档站中英同步、编辑器 schema 重新生成；**探针未实跑**；`SpiritRoot.element` 是破坏性改名） |
| `50_目标选择器与轮盘扩展点设计.md` | 两个扩展点：**目标选择器**（新增 `mxt:ray` / `mxt:cone` 两个几何选择器，`limit` / `order` 作为三种范围选择器共用的筛选，射线与锥形沿中心线做方块裁剪、不穿墙）、**轮盘**（`api/WheelSource` / `api/WheelEntryKind` 从枚举改成接口 + `WheelSourceTypes` / `WheelEntryKinds` 代码注册表，kind 自己拥有 `trigger`，provider 按来源 id 多槽位注册，请求与存档传 id 并容错解析） | **已落地**（2026-09-25；三处编译通过、lang 键一致、文档站中英同步、编辑器 schema 重新生成；**两条新探针腿未实跑**；删除了两个 runtime 枚举） |
| `51_寿元闭环设计.md` | 审计 **A2**：把寿元做成闭环——附件记**两个数**（剩余 + 上限），来源给两条（新实体行为 `mxt:modify_lifespan` + `realm_stage.lifespan` 突破加寿），耗尽后果三选一（`NONE` / `DEATH`（默认：**玩家转为旁观者**，非玩家生物走新伤害类型 `mxt:lifespan`）/ `REINCARNATE`（洗修为境界，保留什么由「转世」页 8 个开关定，默认留魂四项与灵根体质与功法）），只在线的刻流逝而倍率用「结算周期 ÷ 每次流逝」两个整数表达，新增服务器配置「寿元」页 7 项 + 「转世」页 8 项（**总开关 `enabled` 默认关**、`base_lifespan` 默认 0＝不播种，所以默认不改变现有行为），外加面板一行、跨阈值提醒、`/lifespan` 命令、`MxtLifespan` 脚本全局、数据包行为 `mxt:reincarnate` 与显式转世的 `LifeSpanRebirthEvent`（`Pre` 可取消 / `Post`）；顺手修掉"每个实体每 tick 都创建一份 `spirit_stats` 附件" | **已落地**（2026-09-25 实现，未提交、未实机；探针只编译） |
| `52_修炼方式设计.md` | **修炼方式（cultivation method）**：把"能不能练、拿什么练"从五处门禁（`Aura.start_cultivate_conditions` / `AuraZone.cultivate_condition` / `RealmStage.cultivate_condition` / `CultivateAction.start_condition`+`condition` / 服务器配置）收成**定义自带的入场要求**，让「仅凭功法修炼（无功法不能练）」与「修炼不受限制」只是同一张 `cultivate_action` 表里要求不同的两条——补 3 个实体条件（`mxt:technique` / `mxt:cultivating` / `mxt:partner`，后者只回答"身边有没有这样的人"、不建持久关系）、2 个实体行为（`mxt:cultivate` / `mxt:stop_cultivating`，数据包唯一缺的起停入口）、选择器从"盲选 `default`"改成"按适用性挑"（存储优先于 `default`、冷却不进适用性、新增 `Failure.NOT_APPLICABLE`）；可选把 `StageConfiguration` 补 `passive_modifiers` / `cultivation_modifier` 以承载素材里的"层数→属性"与"层数决定速度"；含素材 `21-修炼功法与双修.md` 的逐条映射、文件级落点清单与"不做"清单 | **设计稿**（2026-09-26 按用户要求先设计，未实现；已否掉"新注册表 `mxt:cultivation_method`"与"服务器配置开关"两个方案） |
| `53_绑定表与物品组件重划设计.md` | **绑定表与原子组件**：六张绑定表一律 `implements ItemMatcher`（定义用 `items` + `priority` **自动认领**物品），载荷拆成 6 个可复用**原子组件**（`mxt:quality_chain` / `mxt:element` / `mxt:pill` / `mxt:technique_reading` / `mxt:forging_methods` / `mxt:forging_blueprints`），物品上不再出现"绑定对象"（`mxt:tool_binding` / `mxt:blueprint_binding` 两个 Holder 组件退场）；条件、`item_binding.actions`、`weapon_binding.attributes` 与武器三个动作**只由定义给**（属性修正的原版出口是 `minecraft:attribute_modifiers`）；定义与组件是同一批原子的两种投放方式，合成规则三句（单值组件优先 / 列表并集 / 记录字段级覆盖）；`technique_binding` 的 `items` 可选且堆上 `mxt:technique` 组件优先，被认领的物品同样能长按阅读；§11 是落地记录（含未做的缓存与探针腿） | **已落地**（2026-09-26；`compileJava`/`compileTestModJava`/`processTestModResources` 通过；文档三处与文档站中英同步、编辑器 schema 重新生成；**探针未实跑、解析缓存未做**） |
| `54_统一状态存储与物品宿主设计.md` | **统一状态存储与物品宿主**：26 个物品组件里只有 `artifact_storage` 是"某条技能的状态"（其余是身份载荷或这一堆自己的状态，多键表塞不进 `(id, 类型)` 单值寻址），能力侧真正重复的是 `AbilityAttachment.cooldowns` 与 `CooldownDataStorage` 的同 tick 双写；结论：**物品堆也成为 `DataStorage` 宿主**（组件 `mxt:storage` 装 `DataStorageHolder`，按「技能 id + 类型类」寻址、写入换值不原地改），`mxt:storage` 技能的容器平移到新类型 `mxt:container`（`slots` 仍留在技能类型上），删组件 `mxt:artifact_storage` 与那张冷却表；物品存储只给运行时（不加物品侧 `modify_storage` 与 storage 条件），地址加 family 与触发器/灵气爆发/修炼三张表的收编留给后续 | **已落地**（2026-09-26；三处编译通过、lang 键未变、文档站中英同步；**探针未实跑**；删掉一个死探针助手并补成真的往返检查） |

### C. 玩法模块

| 稿                    | 一句话                                                                                          | 状态                                                                                                                                                                             |
|----------------------|----------------------------------------------------------------------------------------------|--------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|
| `15_子区块灵气缓存误差研究.md`  | 量化"方块塌缩到子区块中心"的误差，给出 15 格 / 28 格两条边界                                                         | 研究结论已用进方块灵气的查询范围                                                                                                                                                               |
| `16_锻造台UI与数据模型修订.md` | 锻造台界面布局定稿、数据模型与运行时修订、限流与实现记录                                                                 | 已落地；两处"未接入"判断已过时（`audit/生产系统审计.md` §4.2）                                                                                                                                       |
| `17_灵气解析性能与记忆化.md`   | 灵气解析按 tick 记忆化 + 按需重算的实测与线上观测                                                                | 已落地                                                                                                                                                                            |
| `18_阵法系统设计.md`       | 阵法类型、基座要提供的基础能力、实现路径与缺陷清单                                                                    | 部分有效：§1 / §5 有效，基座侧 3 处判断与宗门领地段落已作废（修订见 `audit/formation.md`）                                                                                                                  |
| `21_元素系统设计补完.md`     | 元素认领伤害类型（伤害的元素通道）、附着与反应等 P0/P1 补完                                                            | 已落地（§9 落地记录，2026-09-20）                                                                                                                                                        |
| `22_秘境重做设计.md`       | `secret_realm`（当时叫 `realm_instance`）重做成"按需创建的实例维度"：生成参数、运行时与迁移                               | 已落地（§10 落地记录；2026-09-23 全仓改名 `secret_realm`，正文保留原写法）                                                                                                                           |
| `23_法器数据重设计.md`      | `mxt:artifact` 的数据形状重做（`items` 认领物品 + `abilities` 固有分派）                                      | 已落地（§10 落地记录；§11 并入原护符槽位稿）                                                                                                                                                     |
| `24_法器长按与认主设计.md`    | 长按认主 / 注灵的字段、判定与代价，共 8 次修订                                                                   | 已落地；读数以最后一节（§17）为准，§11–§16 是历史层                                                                                                                                                |
| `26_可拖动HUD框架设计.md`   | 从 KronHUD 一系移植的可拖动 HUD 框架（去掉缩放）与已接入元素                                                        | 已落地（实机观感未验）                                                                                                                                                                    |
| `27_轮盘选择系统设计.md`     | 12 扇径向轮盘的几何、扇环渲染、开合状态机与交互；§6–§10 是同日续改                                                       | 部分有效：被 `28` 取代的是"内容来源"，几何与交互不变                                                                                                                                                 |
| `28_技能与灵气归一化设计.md`   | 技能与灵气归一成轮盘条目、12 格内容来源、服务端校验                                                                  | 已落地（取代 `27` 的内容来源）                                                                                                                                                             |
| `29_伤害加成口径与直通标签.md`  | 无归属伤害的加成口径与 `mxt:no_bonus` 直通标签                                                              | 已落地                                                                                                                                                                            |
| `31_多轮盘与轮盘来源设计.md`   | 一个玩家几张盘、每张盘的内容从哪来、请求怎么被服务端认下来                                                                | 已落地（含同日两轮修正）                                                                                                                                                                   |
| `32_法器开关与轮盘接线设计.md`  | 法器技能（`ToggableArtifactAbility`）与轮盘接线                                                         | **部分有效**：`ToggableArtifactAbility` / `ArtifactToggleService` / `WheelEntryKind.ARTIFACT` 已于 2026-09-23 被 `41` 取代（升格为通用 `Toggable` + `AbilityActivationService`）；文内推理与储物界面结论仍成立；§2.3 与 §5 的**主盘池子口径**已于 2026-09-26 收紧——法器开关只有 **Curios 槽**上的可钉，主手 / 副手声明的只留在自己那张从盘页上 |
| `35_御剑飞行系统设计.md`     | 通用御剑飞行：`mxt:flight` 加可选 `mount`（外形/尺寸/乘客位置/步高/坐姿）与 `custody`，载具实体的同步、朝向与渲染器，生命周期出口清单与 7 处待拍板 | **部分有效**：§2 表格第 1/3/7 行与 §6.1 的 `mount` / `custody` 字段形状已于 2026-09-25 被 `48` 取代（改由"法器侧 `mxt:mount` 只描述车 + 功法侧 `mxt:flight_control` 才是按键格"承担，托管不做）；「不做/推迟」与外部工程对照、生命周期出口清单仍有效（未落地；含 3 条只读代码推断未实机） |
| `47_生物与契约接口设计.md`   | 生物模块接口化：契约资格从数据（`creature_profile.contract_tags`）改成代码（实体实现 `Contractable` / `ContractOperations`），捕捉改成物品自己的规则、实体只收 `CaptureListener` 通知；主人交给原版 `OwnableEntity`；含 `contract_type` 的 `costs` / `max_owned` / `recall_cooldown`、主世界附件 `bound_beasts` 主人索引与 `/mxt contract` 命令；§14 追加契约兽的**行为**（`ContractBehavior` 类 + `ContractBehaviors` 装载，不是 enum）与御兽铃的轮盘输入；§11–§13 是落地记录与三处追加 | **已落地**（2026-09-24，§14 于 2026-09-25 追加；`compileJava`/`compileTestModJava`/`processTestModResources` 通过；两份 lang 一致；文档三处与文档站中英已同步；**`/mxt_test contract` 探针未实跑**）                                                                                                                                     |
| `48_飞行法器与御器术重设计.md` | **飞行法器重做**：一个概念拆两条技能——法器侧的 `mxt:mount` **只描述车**（`speed` / `seats` / `sit` / `display` / 几何 / 每 tick 燃料），功法（或任何授予来源）侧的 `mxt:flight_control` 才是"能不能御器"的**按键格**（起剑一次的价格、冷却、条件、`hand`、`speed_multiplier`）；起剑时那件法器被**收进载具实体**（落地原样归还），载具从**主手/副手**取、多人座位与右键上座、几何改由定义给（覆写点对着 26.1.2 原版源码逐条核过）、附件与生命周期出口按新形状重列 | **已落地**（2026-09-25；`compileJava`/`compileTestModJava`/`processTestModResources` 通过；文档三处与文档站中英已同步；结构编辑器 schema 重新生成、checker `no problems found`；**探针未实跑**）。§14 是落地记录（含同日追加三件：碰撞真的生效、`FlightToggleC2SPayload` 并入通用 payload、`mxt:mount` 读不到的字段改加载期报错）；推翻 `35` §2 第 1/3/7 行与 §6.1 的字段形状                         |

### D. 分析与参考

| 稿                                | 一句话                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | 状态                                                                                                                                                                    |
|----------------------------------|------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|-----------------------------------------------------------------------------------------------------------------------------------------------------------------------|
| `12_外部内容接入可行性分析.md`              | 对照外部策划文档说明各功能的落点与边界                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | 分析稿；宗门相关行已作废                                                                                                                                                          |
| `19_FTB_Teams与FTB_Chunks接入分析.md` | FTB Teams / Chunks 的接线形态与取舍                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | 已按此接入（`compat/ftb/**`；FTB 依赖是 `compileOnly`）                                                                                                                          |
| `20_原版创造模式的信任模型.md`              | 反编译原版源码看创造模式物品怎么进背包、服务端三道闸门                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | 研究稿；`/picker` 的信任模型照它写                                                                                                                                                |
| `30_元素与物品通道可行性分析.md`             | 物品元素、元素来源区分、法宝抵消、`damage_types` 自带附着四项                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | 部分已落地（2026-09-22 三个批次有落地记录），其余待拍板                                                                                                                                     |
| `40_能力与法器能力合并设计.md`              | **可行性 + 合并定案 + 同日三次收缩（一份稿；2026-09-23 并入原 `40` / `42` / `44` 三份稿）**：先讲清 `Ability` 与 `ArtifactAbility` 差在哪（三处不可通约：状态在持有者 / 物品、身份是注册表 id / 物品堆、驱动是一次施放 / 状态机与界面）与当时的四个方案（**附录 A 全文保留**，含"并入 `AbilityType` 当时判不建议、最后被采纳"的来龙去脉）；再给出定案——只留 `mxt:ability` 一个概念（`mxt:passive`/`mxt:active` 退役、`mxt:flight`/`storage`/`upkeep` 变成普通类型），`ToggableArtifactAbility` 升级为与宿主无关的 `Toggable` + 唯一入口 `AbilityActivationService`，冷却按能力实例独立，法器 `abilities` 只收技能 id 或 `#技能标签`，身份就是它自己的 `Holder<Ability>`；含 Origins 157 个内建 power 的只读普查与 P0–P2 分期 | **已落地**（2026-09-23；`compileJava`/`compileTestModJava`/`processTestModResources` 通过；文档三处与文档站中英已同步；**未实机**）。同日三次收缩（取消内联 / 取消 `AbilityRef` / 冷却改画轮盘格）记在 §12，第三次的细节见 `41` |
| `41_技能冷却改用原版物品渲染.md`             | **收缩稿**：删掉左上角「技能冷却」那一行 HUD 元素（选中的技能在轮盘格上已有金框，不必再说第二遍），冷却改成**画在轮盘格上**——照原版物品冷却画 50% 白幕、盖住图标的剩余比例、随时间向下退；全长新增 `WheelMenuEntry#cooldownLength`（技能取上一次实际拿到的 `mxt:cooldown` 长度、灵气取发射间隔），答不出全长就画满整块；含"编辑器里根本没有隐藏开关"这处文档不实的修正                                                                                                                                                                                                                                                                                                       | **已落地**（2026-09-23，`compileJava`/`compileTestModJava` 通过；两份 lang 740 键一致；文档三处与文档站中英已同步；**未实机**）                                                                       |

### E. 实现留档与原始材料

| 文件                                                          | 一句话                              | 状态                  |
|-------------------------------------------------------------|----------------------------------|---------------------|
| `方块灵气实现.md`                                                 | 一份外部架构提案的留档（含占位包名）＋末节"当前工程的实现映射" | 素材：实现以代码为准          |
| `裂缝渲染实现.md`                                                 | 裂隙方块的数据模型、放置、渲染管线、着色器与两次改版记录     | 已落地（§11 / §12 落地记录） |
| `原始需求清单.txt`                                                | 最初那份模块愿望清单（`00` 的输入）             | 素材                  |
| `aura_subchunk_error.py` / `.png`、`aura_subchunk_value.png` | `15` 的误差实验脚本与配图                  | 素材                  |
| `rift_mesh_preview.py` / `.png`                             | `裂缝渲染实现.md` 的网格预览                | 素材                  |

### F. 审计与测试设定

| 文件                                    | 一句话                                                                          |
|---------------------------------------|------------------------------------------------------------------------------|
| `audit/生产系统审计.md`                     | 锻造 / 炼丹 / 灵草 / 灵材合成 / 符箓的实现与缺口；文末"文档与代码不一致"还标了**研究稿自身**的过时点                  |
| `audit/基座缺口审计.md`                  | **对着 HEAD `2ecbccd` 复核过的基座缺口清单（合计 20 项，已关闭 17 项，仍开放 3 项：B3 · B5 · B6，🔴 0 · 🟠 2；2026-09-25 关闭了 A1 / A2 / A3 / B2 / B9 与 C 组 11 项，2026-09-26 关闭 B4）**：外部承载能力审计（12 个内容模组 × 19 个功能域）的 `incomplete.md` 逐条重核后的落地版，含每一条的 HEAD 证据、**需要补全的内容**、影响与替代，以及原 3 项"未确认"的复核结论（1 项转为事实、1 项被推翻）；只登记、不实现 |
| `audit/formation.md`                  | 阵法落地设计：对照 `18` 与当前代码，修订 3 处过时判断并重写缺口方案                                       |
| `audit/resource-cultivation-split.md` | `resource` / `cultivation` 拆分的破坏性变更记要                                        |
| `audit/spirit.md`                     | 灵石灌注（`AuraItemAccess` 反向使用 `item_aura`）审计                                    |
| udit/talisman.md                     | 符箓灌注与激发审计                                                                    |
| `audit/secret-realm-rename.md`        | 秘境一系从 `realm_instance` 改成 `secret_realm` 的破坏性改名记要：完整映射、受影响的 id 与存档格式、保留不动的名字 |
| `audit/technique.md`                  | 功法数据驱动与功能审计                                                                  |
| `测试设定参考/`                             | 来源与阅读记录、常见设定测试面、集成测试场景矩阵                                                     |

### G. 外部工程阅读

| 文件                    | 一句话                                                                                                                      | 状态                   |
|-----------------------|--------------------------------------------------------------------------------------------------------------------------|----------------------|
| `immortal/傀儡与符文编程.md` | Immortal（外部工程）的傀儡 AI：行为节点 + 共享记忆黑板（槽序即优先级）+ 指示符文与逻辑门拼出的条件树 + 合成/逻辑/绑定三页装配台；含它自己在 1.21 迁移里被注释停用的清单（数据层、屏幕注册、创造栏）与本仓库的落点取舍 | 分析 / 研究（只读源码；未编译未实机） |
| `immortal/符箓与法术刻印.md` | Immortal 的符箓：「法术 + 等级 + 触发时机」刻在物品上、10 种触发条件各自声明扫哪些装备槽、法术↔物品的反向绑定；含与本仓库已有符箓实现的取舍                                          | 分析 / 研究（只读源码；未编译未实机） |

### 几条主线（按线读，不按号读）

- **法器 / 能力内核**：`23`（数据重做）→ `24`（长按认主）→ `32`（技能与轮盘接线，§7 储物）→ **`40`（一份稿走完：可行性分析与四个方案（附录
  A）→ 合并定案（一个 `Ability` + 通用 `Toggable` + 独立冷却）→ 同日三次收缩（取消内联、取消 `AbilityRef`、冷却改画轮盘格））
  ** → `41`（冷却的显示出口）。
- **飞行 / 载具**：`23`（`mxt:flight` 的数据形状）→ `32`（轮盘上那一格入口）→ `35`（载具的外观、几何与生命周期出口，未落地）→ **`48`（重设计：法器侧 `mxt:mount` 只描述车、功法侧 `mxt:flight_control` 才是能不能御器的按键格；主副手取用、多座位、几何改由定义给，未落地）**。
- **轮盘**：`27`（几何与交互）→ `28`（内容归一化，取代 `27` 的内容来源）→ `31`（多盘与来源）→ `32`（法器技能接进来源）→ `41`
  （冷却不再单起一行、改画在轮盘格上）。
- **伤害与元素**：`21`（元素补完，含附着与反应）→ `29`（加成口径与直通标签）→ `30`（物品与元素通道，分批落地）。
- **灵气**：`15`（缓存误差）→ `17`（解析记忆化）→ `方块灵气实现.md`（方块来源与查询范围映射）。
- **能力与触发内核**：`05`（能力模型）→ `33`（Trigger 分层与订阅重建）→ `40`（与法器能力的统一与合并，已落地）→ 各玩法模块按需复用。
- **生物 / 契约**：`47`（资格归代码、行为归实体；未落地，待拍板）。

## 什么时候必须写

- **大改**：新增一个模块、注册表、系统，或对现有系统做**换形状**的改版（哪怕最后没做）。
- **用户明确要求先设计**（说"先写份设计稿 / 方案"就是）。
- 做出会影响数据包格式、公开 API 或运行时语义的决定——包括**推翻**以前的设计。
- 真做过一次系统普查式的审计（结论放 `audit/`）。

不需要写，也别为一个改动顺手补一篇：修 bug、改名、拆包、加字段、补一句 Javadoc、文档同步、纯结构调整这类日常改动。
它们的结论写进代码注释（硬约束）与 `docs/`、[`AGENTS.md`](../AGENTS.md)（约定）即可；**一次改动不等于一次审计**。

## 放哪里、怎么命名

| 内容                    | 位置          | 命名                                                    |
|-----------------------|-------------|-------------------------------------------------------|
| 设计稿 / 方案 / 改版         | 本目录根下       | `NN_<主题>设计.md`，`NN` 取当前最大编号加一                         |
| 实现说明（已经落地的机制怎么跑）      | 本目录根下       | `<模块>实现.md`（如 `裂缝渲染实现.md`；`方块灵气实现.md` 沿用这个名字，内容是提案留档） |
| 审计与差距清单               | `audit/`    | 按模块或主题命名（`technique.md`、`formation.md`、`基座缺口审计.md`…） |
| 测试设定参考                | `测试设定参考/`   | 场景矩阵、常见设定与来源记录                                        |
| 研究用的脚本与配图             | 本目录根下       | 与它服务的文档同名（`aura_subchunk_value.py` / `.png`）          |
| 外部工程阅读（Immortal 等）    | `immortal/` | `<主题>.md`（如 `傀儡与符文编程.md`），**不占编号**                    |
| 清单 / 备忘 / 原始素材（不是设计稿） | 本目录根下       | 中文名说清是什么（`原始需求清单.txt`、`备忘录.md`），**不占编号**              |

写进去的稿子要能独立读懂：**基准**（对着哪份代码/文档写的）、**结论**、**与现状的差异**、**不做或推迟的部分**、**验证方式**
。设计被推翻时不要删旧稿，在原文上标注"已于 <日期> 被 <新稿/提交> 推翻"并写清新行为——旧结论同样是资产，`audit/` 里就是这么做的。

## 已退役的稿

| 稿                             | 是什么                                                                              | 去向                                                                                                                                                     |
|-------------------------------|----------------------------------------------------------------------------------|--------------------------------------------------------------------------------------------------------------------------------------------------------|
| `13_Mine-And-Slash功能参考.md`    | Mine & Slash（1.20 Forge）的功能层阅读与"明确不移植"清单                                         | **2026-09-22 删除**（未采用；唯一被引用的结论"职业等级本身可以不移植"在 `audit/technique.md` §8.6，已改为直接引原句）。文件仍在 git 历史里：`git show HEAD:research/13_Mine-And-Slash功能参考.md`        |
| `14_Immortal功能与AI分析.md`       | Immortal 的功能支撑矩阵与可借鉴设计（元素附着与反应、法器刻印、分页秘籍、傀儡 AI、修士 NPC 档案）                        | **2026-09-22 删除**（未采用；其中"元素附着与反应"的参考价值后来由 `21_元素系统设计补完.md` §9 落地实现，其余条目至今没有对应实现）。同样可从 git 历史取回；其中"傀儡 AI"与"符箓刻印"两条线的源码级阅读另见 `immortal/` 下两篇（不占编号）       |
| `24_护符槽位设计.md`                | Curios 内置 `charm` 四格                                                             | 2026-09-21 整体并入 `23_法器数据重设计.md` §11 后删除；同日这个号被 `24_法器长按与认主设计.md` 复用                                                                                    |
| `25_item_type字段删除.md`         | 法器 `item_type` 字段的删除理由与替代写法                                                      | 2026-09-22 整理时确认**已不存在，且从未进过 git**（只在工作树里存在过），理由无法取回 ⇒ **编号 `25_` 是空的**。`docs/模块实现审计.md` 法器行里那条引用已标注失效                                                 |
| `29_轮盘选中与使用设计.md`             | 轮盘的选中与使用（R 选 / V 用、选中持久化、HUD 十二格、落空提示）                                           | 2026-09-22 整体并入 `27_轮盘选择系统设计.md` §6–§10 后删除（27 文内有标注）；该号现由 `29_伤害加成口径与直通标签.md` 使用                                                                      |
| `TriggerSystem.md`、`list.txt` | 编号制度之前写的稿与清单                                                                     | 2026-09-22 **改名**为 `33_Trigger系统设计.md`、`原始需求清单.txt`（正文未改；编号为补发）                                                                                        |
| `34_对外API包设计.md`              | 对外 Java API 的包边界：哪些契约搬进 `com.iafenvoy.mxt.api`、服务类代理为什么推迟                        | 2026-09-22 在注释整理提交里**被删除**；结论并入 `AGENTS.md` §3 与 [`docs/guide/java/interfaces.md`](../docs/guide/java/interfaces.md)。编号 `34_` 现由 `34_功法载体物品组件设计.md` 使用 |
| `35_注释口径与全仓瘦身.md`             | 注释三律、保留与删除判定表、全仓瘦身结果                                                             | 2026-09-22 **从未进过 git，现已不在工作树**；权威表述在 `AGENTS.md` §4「注释三律」与 `docs/SKILL.md`。该号现由 `35_御剑飞行系统设计.md` 使用                                                   |
| `42`–`45`（命令参数 / 附件 codec / 身份命令 / 命令分包 四篇重构稿） | 2026-09-23 四轮改动的设计稿：命令注册表参数改用 `ResourceArgument`、附件 codec 改宽容读取 + 注册表引用存 `Holder`/`ResourceKey`、`/identity` 拆成 `/spirit_root` + `/physique`（附带 `remove` 对停用条目失效的修正）、命令包按侧分包 + 双端管理器（`mayChange` 唯一化） | **2026-09-23 按要求删除**：这类改动不必逐次留档，结论已落在 `AGENTS.md` §4 房规、`docs/模块实现审计.md`（该文档已于 2026-09-25 一并删除，见 [`docs/README.md`](../docs/README.md) 的退役记录）与代码注释里。四篇**从未进过提交**（只在本轮变更索引里存在过）；**编号 42–45 视为已用**，新稿从 `46_` 起排 |
| `audit/content-coverage-gaps.md`  | 内容表对照基座的**缺口清单**（逐条编号，多数已标"已于 <日期> 关闭"；516 行）                                   | **2026-09-25 按要求删除**，由 `audit/基座缺口审计.md` 取代（那篇是对着 HEAD `2ecbccd` 逐条重核、并写清"需要补什么"的版本）。git 历史可取回：`git show HEAD:research/audit/content-coverage-gaps.md`。`18`/`21`/`23`/`46`/`47` 等设计稿与 `audit/` 各篇里对它的引用**按本目录既有约定保留不改**（那是当时的证据） |

> `audit/` 里的旧结论常引用 `docs/通用物品.md:23`、`docs/灵气环境数据包.md:84` 这类**当时**的文档行号作证据；也有的引用上面退役的稿。那
> 5 篇迁移前的散篇已于 2026-09-21 从 `docs/` 删除（内容并入 `docs/数据包格式.md`、`docs/guide/**` 与文档站，清单见 [
`docs/README.md`](../docs/README.md)）。**结论按当时的证据成立，不必回头改**；要复核就看代码与现行文档。

## 与其他目录的分工

| 目录                                                                 | 放什么                                              | 不放什么                                                   |
|--------------------------------------------------------------------|--------------------------------------------------|--------------------------------------------------------|
| `research/`（这里）                                                    | 设计意图、方案对比、审计结论、推翻记录                              | 面向使用者的字段说明                                             |
| `docs/`                                                            | 数据包 / KubeJS / Java 的用法文档，字段级权威在 `docs/数据包格式.md` | 未定稿的设想                                                 |
| 文档站仓库（[`IAFEnvoy/mxt-docs`](https://github.com/IAFEnvoy/mxt-docs)） | 玩家看的整理与教程（中英）                                    | 设计过程                                                   |
| `src/main/java`                                                    | 实现                                               | "为什么"（属于这里与 `docs/`；类注释只留结论与硬约束，见 `AGENTS.md` §4 注释三律） |

数据包字段一旦定稿，说明进 `docs/数据包格式.md`；玩家侧的说法进文档站；**设计过程留在这里**。仓库根目录的 [
`AGENTS.md`](../AGENTS.md) 有完整的开工约定。
