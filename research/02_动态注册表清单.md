# 动态注册表清单

> - 2026-09-16：**新增 `talisman` 数据包注册表与符箓载体**。`mxt:talisman` 物品不再是空壳：新增 `mxt/talisman` 注册表（`Talisman` 记录，目前只有 `abilities` 与 `aura_cost` 两个可选字段），并新增物品组件 `mxt:talisman`（`TalismanComponent`，按追加顺序保存已铭刻的 `Holder<Talisman>` 列表，空列表即刚做出来的空载体）。理由：制符闭环需要一个"做出来的符箓落在哪"的载体，先把注册表和组件建好，铭刻服务与其余字段后续再加。字段只保留 `abilities`（授予的能力）与 `aura_cost`（激发所需的灵气账单），是因为本模组所有技能性效果都已经落在 `ability` 上，先写一套符箓专属的效果词汇会在玩法定型前锁死格式；后续字段一律作为**可选**字段加入，今天写好的 JSON 继续可解析。`aura_cost` 采用与 `cultivate_action.aura_costs`、`formation.storage.capacity` 相同的 `Map<Holder<Aura>, NumberProvider>` 形状（2026-09-17 起键是灵气；此前是 `Holder<Resource>`），值可以是常量或公式；它只收具体 aura 而不收标签，因为灵气池本身就按具体 aura 分池。代价：多一张注册表、多一个物品组件，且 `talisman` 定义目前没有任何运行时消费者（组件只保存和展示，`aura_cost` 也还没有人收账）。影响面：数据包注册表 33 → 34，`mxt` 创造栏多一个 `mxt:talisman`（暂未做贴图，显示缺失贴图占位）。
>
> - 2026-09-16：**删除 `title` 与 `badge` 预留**。称号与徽章不再做，`title`、`badge` 两个数据包注册表与 `badge_type` 固有注册表从代码中彻底删除，本清单里的 `title` 行随之移除。理由：两者都停在"结构与 Codec 可加载"的状态，没有任何玩法消费者（徽章连渲染器都没有），属于长期挂账的预留；预留在注册表和附件里持续产生维护成本——`SpiritIdentityAttachment` 多一个 `titles` 持久化字段、`PassiveAttributeService` 多一段被动属性汇总、数据包文档和测试夹具都要为它们维持描述。代价：旧存档附件里的 `titles` 字段被忽略，称号数据不再保留，项目尚未发布因而不提供迁移。`BadgeCodecs.TRANSLATABLE_COMPONENT` 被 `item_quality` 与 `currency` 用到，已迁到 `util.codec.MiscCodecs`，没有随徽章一起删除。影响面：数据包注册表 35 → 33，固有注册表少一个 `badge_type`（它本来就不开 `mxt:js`，因此脚本扩展点表不变）。

## 通用约定

所有内容定义采用数据包路径 `data/<namespace>/mxt/<registry_name>/<path>.json`，其资源 ID 为 `<namespace>:<path>`。Java
启动时建立内置默认空注册表，数据包重载时原子替换快照，并向客户端同步可见定义与版本号。

每个定义至少包含：

```json
{
  "schema_version": 1,
  "id": "example:entry",
  "enabled": true,
  "tags": [
    "example:tag"
  ]
}
```

`id` 可由文件路径推导；若同时存在，加载器必须校验二者一致。引用其他定义时只保存资源 ID。数值使用有界校验；未知字段保留给扩展，不应造成整个数据包重载失败。

固有注册表与数据包注册表的协作、生命周期和两种联动模式见 `08_注册表协作规范.md`。本文件仅列出数据包定义的内容类型。

## 必须实现的注册表

| 注册表                     | ID 类型  | 主要字段                                                                              | 运行时消费者                                      |
|-------------------------|--------|-----------------------------------------------------------------------------------|---------------------------------------------|
| `realm_stage`           | 境界     | 所属 `cultivation` 档案、进度阈值、突破条件、被动修正、能力门槛、劫数 ID                                                      | `cultivation`、`tribulation`、`ability`。      |
| `element`               | 元素     | 显示、元素关系、环境偏向标签、克制/适配规则                                                            | `cultivation`、`ability`、`world`。            |
| `spirit_root`           | 灵根     | 唯一 `element` 引用、元素相关修炼倍率、元素法术适配、稀有度                                               | `cultivation`、`ability`、世界生成。               |
| `physique`              | 体质     | 独立属性修正、授予的 `ability`、持有条件、互斥标签、稀有度                                                | `cultivation`、`ability`。                    |
| `resource`              | 通用资源   | 默认值、最小值、上限、图标、粒子色、内联资源条（只负责存储与展示）                                                  | `resource`、`ability`、其他数值消费者。 |
| `cultivation`           | 修炼档案   | 一对一引用 `resource`：境界入口、凡人阈值与开始条件、自然恢复、灵气标记、灵力射线、修为双向换算、可用性门禁、信息面板开关                          | `resource`、`realm_stage`、`element`。 |
| `resource_bar`          | 资源显示条  | 绑定的 `resource`、可见性、锚点、排序、`renderer.type` 与渲染参数                                    | `client`、`resource`。                        |
| `curse`                 | 诅咒     | 固有 `type`、显示、施加条件、叠层/持续规则、周期/到期/解毒动作、标签（解毒由解毒剂 + `mxt:curse` 标签管理）                                         | `curse`、`ability`、`artifact`、所有生命周期消费者。     |
| `ability`               | 统一能力   | 固有 `type`、显示、条件、全部业务数值的 `NumberProvider`/exp4j 公式、`costs` 资源成本数组、动作、触发器、状态组件配置、标签 | `ability`、`resource`。                       |
| `technique` | 功法     | 品级、学习条件、互斥标签、修炼修正、授予的 `ability` 列表、默认水平 `default_stage`、熟练度数值 `mastery_resource`、每级的 `configuration`（到达条件 + 该级能力）                  | `cultivation`、`ability`、`skill_stage`、`resource`。      |
| `trigger`               | 事件规则   | 信号匹配器 `trigger`、条件 `condition`、行为 `action`；独立于技能，把任意已发布信号变成效果                          | `trigger_type`、`condition`、`action`。        |
| `talisman`              | 符箓     | 激发时授予的 `ability` 列表（`HolderOrTag`，可写单个 ID、数组或 `#标签`）；激发所需的灵气账单 `aura_cost`（`Map<Holder<Aura>, NumberProvider>`，只收具体 aura） | `talisman` 物品组件（只保存与展示）。               |
| `skill_stage`           | 技能水平   | 链身份 `skill`、单向 `next_stage`、到达所需熟练度 `mastery`、`damage_multiplier`                                  | `cultivation`、`ability`。                    |
| `cultivate_action`      | 修炼动作   | 启动/停止条件、tick 周期、环境要求、收益动作                                                         | `cultivation`。**已标记为将来可能移除**（2026-09-17，`CultivateAction` 的 `//TODO::`）。 |
| `item_archetype`        | 修真物品原型 | 物品类型、法宝容量、祭炼规则、储物规格、飞剑参数、授予的 `ability` 列表                                         | `artifact`。                                 |
| `forging_method`        | 锻打方式   | 值变化、可用条件、表现和 `costs` 消耗数组                                                         | `forging`、`resource`。                       |
| `forging_blueprint`     | 锻造蓝图   | 输入、允许方式、数值条边界、成功区间、末尾序列规则、品质曲线                                                    | `forging`、`artifact`。                       |
| `spirit_herb`           | 灵药     | 品质、年份、元素/材料标签、成长和掉落参数                                                             | `alchemy`、`world`。                          |
| `alchemy_recipe`        | 丹方     | 输入、温度曲线、炉级、时长、成功/失败产物、结果动作                                                        | `alchemy`。                                  |
| `formation`             | 阵法     | 结构模板、激活资源、半径、`costs` 激活/维护消耗、激活/维持/触发动作                                           | `formation`、`world`、`resource`。             |
| `tribulation`           | 劫数     | 触发条件、阶段、持续时间、难度缩放、阶段/结算动作                                                         | `tribulation`。                              |
| `creature_profile`      | 妖兽资料   | 境界、灵智、实体条件、内丹/掉落表、可契约标签                                                         | `creature`、`world`。                         |
| `contract_type`         | 契约     | 双方限制、跟随/协战策略、解除与惩罚动作                                                              | `creature`。                                 |
| `item_quality`          | 统一品质   | 显示、价值/锻造/炼丹数值系数；排序与分组由原版标签控制                                                    | `artifact`、`alchemy`、`forging`、物品绑定。        |

## 应通过标签而非独立注册表表达的内容

- 元素分组：`element` 注册表定义元素及关系；标签可用于表达五行、变异元素或可被某类法术匹配的元素集合。
- 流派与限制：剑修、体修、丹修、器修、符修、阵修、魔修、兽修。
- 物品分类：丹药、灵药、阵旗、灵石、储物、法器、灵宝、武器、服装。
- 生物和世界条件：妖兽、可契约、灵气富集生物群系、灵脉候选区块。

标签可被原版/第三方物品和实体复用，避免为每种武器、材料和元素组合生成注册表条目。

## KubeJS 与数据包合并规则

1. 数据包在重载阶段加载，KubeJS 在约定的注册阶段提交构建器产物；两者统一转为不可变定义对象。
2. 同一 ID 只能有一个最终定义。默认规则为数据包覆盖内置定义，KubeJS 覆盖同命名空间的脚本定义；跨来源冲突须记录明确错误，不能静默随机取值。
3. 动态定义只能变更静态配置，不能直接写入其他玩家的附件或世界状态。
4. 每个定义字段中涉及执行逻辑的部分使用固有类型的 `type` 和其 Codec 参数，或引用另一个数据包定义；不得把任意函数名字符串当作执行入口。
5. 热重载后，对正在吟唱、炼丹、渡劫、运行阵法的实例按 ID 重新解析；定义消失时安全中止并返还由配置标注为可返还的资源。

## 不应做成动态注册表的内容

- 附件字段、网络包、存档编码和服务端权限规则。
- 事件的生命周期顺序。
- 高频运行时实例，例如每个阵法、每次施法、每个灵宠契约。
- GUI 控件和可视化编辑器配置。
- 能力、动作、条件、触发器和状态组件的 `type`；它们是 Java 固有注册表条目，见 `05_技能系统与固有注册表.md`。

